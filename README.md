# DocIntel — AI Document Intelligence

Multi-tenant SaaS for asking natural-language questions over your own documents
using retrieval-augmented generation (RAG). ASP.NET Core (.NET 8) API + Angular,
with a pluggable AI layer that runs offline by default.

Status: work in progress.
